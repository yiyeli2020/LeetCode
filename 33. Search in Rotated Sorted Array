class Solution {
public:
    int search(vector<int>& nums, int target) {
       
        int index=-1,len=nums.size();
        for(int i=0;i<len;i++){
            if(nums[i]==target) index=i;
        }
        
        return index;
    }
};
